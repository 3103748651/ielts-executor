name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Patch material links
        run: python tools/patch_index.py
      - name: Build unsigned release APK
        run: gradle assembleRelease --stacktrace
      - name: Collect APK and signing tool
        run: |
          cp app/build/outputs/apk/release/app-release-unsigned.apk IELTS-Executor-v1.3.2-unsigned.apk
          BT=$(ls -d "$ANDROID_HOME"/build-tools/* | sort -V | tail -1)
          echo "Using build-tools: $BT"
          cp "$BT/apksigner" ./apksigner
          cp "$BT/lib/apksigner.jar" ./apksigner.jar
      - uses: actions/upload-artifact@v4
        with:
          name: IELTS-Executor-Android-v1.3.2-unsigned
          path: |
            IELTS-Executor-v1.3.2-unsigned.apk
            apksigner
            apksigner.jar
